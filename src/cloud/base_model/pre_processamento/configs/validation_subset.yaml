# Validation Config for QuantGod Subset
paths:
  rclone_mount: "Z:\\PROJETOS\\BTC_USDT_L2_2023_2026\\btcusdt_L2_2026_test"
  # rclone_config: "rclone.conf" # Not needed for local path
  processed_output: "data/L2/pre_processed_test"
  scaler_path: "data/artifacts/scaler_test.pkl"

etl:
  sampling_interval_ms: 1000  # 1s
  resampling_interval: "1min"
  orderbook_levels: 200      # Hard Cut 200
  compression: "snappy"
  batch_size_zips: 10        # Number of zips to process in a single batch

features:
  normalization: "log_returns" # log_returns, z-score, or both
  apply_zscore: false
