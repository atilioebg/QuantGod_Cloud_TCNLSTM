# =============================================================================
# requirements_paper_trading.txt
# Dependências MÍNIMAS para rodar o Paper Trading em produção (RunPod / Cloud)
# NÃO inclui: optuna, torchvision, torchaudio, matplotlib, seaborn, pytest
# =============================================================================

# Core Data Processing
polars>=1.0.0
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0

# Scikit-learn (necessário para carregar StandardScaler nos .pkl)
scikit-learn>=1.4.0

# Deep Learning — Inference only (CPU ou CUDA, dependendo da imagem)
# Para GPU no RunPod: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0

# Auditor Meta-Model
xgboost>=2.0.0

# Scaler / Serialization
joblib>=1.3.0

# Real-time WebSocket / REST
aiohttp>=3.9.0
websockets>=12.0

# Config
pyyaml>=6.0.1

# Utilities
psutil>=5.9.0
